PORT=3000
DERIV_APP_ID=1089
DERIV_API_TOKEN=
DASHBOARD_USER=admin
DASHBOARD_PASS=UseAVeryStrongPasswordHere

app.use(
  basicAuth({
    users: {
      [process.env.DASHBOARD_USER || "admin"]: process.env.DASHBOARD_PASS || "ChangeMeNow"
    },
    challenge: true
  })
);